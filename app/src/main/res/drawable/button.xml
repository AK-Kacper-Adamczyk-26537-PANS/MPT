<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Ustawienie zaokrąglonych rogów -->
    <corners android:radius="5dp" />

    <!-- Wypełnienie przycisku -->
    <solid android:color="@color/buttonBackground" /> <!-- Kolor wypełnienia -->

    <!-- Obramowanie (border) -->
    <stroke
        android:width="1dp"
        android:color="@color/buttonBorder" /> <!-- Kolor obramowania -->
</shape>
